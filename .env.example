# Optional but recommended for production (free tier: https://finnhub.io/register )
# Without this, quotes use Yahoo Finance — works locally via Vite proxy; many hosts block browser→Yahoo.
VITE_FINNHUB_API_KEY=
